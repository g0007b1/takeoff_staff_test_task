import {Contacts} from "../../components/Contacts";
import {Container} from "@mui/material";

export const ContactsPage = () => {
    return (
        <Container>
            <Contacts/>
        </Container>
    )
}